---
id: sol-20260804-421d52ad
kind: SolutionPortfolio
version: 2
status: active
title: Solutions for: S03 — Prepare the first essay candidate
mode: standard
created_at: 2026-08-04T13:02:02Z
updated_at: 2026-08-04T13:02:43Z
links:
  - ref: prob-20260804-97ecd928
    type: based_on
---

# Solutions for: S03 — Prepare the first essay candidate

Problem: prob-20260804-97ecd928

## Variants (2)

### V1-semantic-acceptance. Semantic/site-acceptance RU normalization depth

RU candidate body is the frontmatter-stripped source body passed through verbatim for this slice's plain-essay scope. No attempt to reproduce exporter-java's specific byte-level quirks (whitespace handling, line-ending normalization, etc.). Link/asset/protected-Markdown transforms are real work deferred to their owning slices (S12-S14), not faked or approximated here.

**Strengths:**
- No new normalization logic to design/test beyond frontmatter stripping (already built in S02's Frontmatter parser)
- Avoids importing legacy quirks nobody has decided are worth preserving
- Keeps the slice minimal per the plan's own slice discipline

**Novelty:** Treats exporter-java strictly as behavioral evidence per the plan's own framing, not a compatibility target — matches the plan's explicit non-goal 'preserving known defects such as bridge schema v3 incompatibility or native-image help failures'.

**Weakest link:** If a later slice's diffing (TRP-02, S08) turns out to be sensitive to exact whitespace/line-ending choices made here, this slice's minimal pass-through may need revisiting rather than being reusable as-is.

**Risks:**
- Future diffing (S08) or byte-for-byte-sensitive consumers could require rework

**Stepping stone:** yes — opens future possibilities

**Stepping-stone basis:** Establishes RU as a pure function of the admitted note's body text, which S12-S14 extend incrementally (comments, links, assets) rather than replace.

### V2-byte-for-byte. Byte-for-byte legacy compatibility

Study and replicate exporter-java's PrepareWorkflow normalization output precisely, including its specific whitespace/line-ending/quirk handling, even for the trivial plain-essay case.

**Strengths:**
- Would make later byte-level comparison against exporter-java output trivial, if ever needed

**Novelty:** Optimizes for compatibility with a system the plan explicitly says is 'not a design template or code donor'.

**Weakest link:** Requires reverse-engineering and locking in legacy quirks before any real evidence (a second kind, a real diff scenario) shows they matter, which is exactly the kind of premature generalization the plan's stop-and-split rules warn against.

**Risks:**
- Locks in undocumented legacy behavior as a requirement with no current consumer
- Contradicts the plan's explicit non-goal of preserving known defects

## Summary

| Variant | Novelty | Diversity Role | Weakest Link | Stepping Stone |
|---------|---------|----------------|-------------|----------------|
| V1-semantic-acceptance. Semantic/site-acceptance RU normalization depth | Treats exporter-java strictly as behavioral evidence per the plan's own framing, not a compatibility target — matches the plan's explicit non-goal 'preserving known defects such as bridge schema v3 incompatibility or native-image help failures'. | - | If a later slice's diffing (TRP-02, S08) turns out to be sensitive to exact whitespace/line-ending choices made here, this slice's minimal pass-through may need revisiting rather than being reusable as-is. | yes |
| V2-byte-for-byte. Byte-for-byte legacy compatibility | Optimizes for compatibility with a system the plan explicitly says is 'not a design template or code donor'. | - | Requires reverse-engineering and locking in legacy quirks before any real evidence (a second kind, a real diff scenario) shows they matter, which is exactly the kind of premature generalization the plan's stop-and-split rules warn against. | no |


## Related History

- [problem] **S03 — Prepare the first essay candidate** `prob-20260804-97ecd928`

## SoTA Survey Reminder

Before deciding, have you surveyed existing solutions?
- **Web search** — industry patterns, blog posts, case studies
- **Library docs** — check current API/usage patterns for relevant libraries
- **FPF spec search** — `haft_query(action="fpf", query="<topic>")` for methodology patterns

## Evidence Collection

Research each variant before comparing. Run tests, check benchmarks, validate claims.
Attach findings: `haft_decision(action="evidence", artifact_ref="sol-20260804-421d52ad", evidence_content="...", evidence_type="research", evidence_verdict="supports")`

## Comparison

| Variant | slice-minimality | plan-alignment | future-rework-risk |
|---------|------|------|------|
| Semantic/site-acceptance RU normalization depth | no new normalization logic beyond frontmatter stripping | matches explicit non-goal against preserving legacy defects | low-medium — only revisited if S08 diffing proves whitespace-sensitive |
| Byte-for-byte legacy compatibility | requires reverse-engineering legacy quirks now | contradicts non-goal; treats oracle as template | locks in undocumented behavior with no current consumer |

## Non-Dominated Set

**Computed Pareto front:** Semantic/site-acceptance RU normalization depth

## Dominated Variant Elimination

- Byte-for-byte legacy compatibility — dominated by Semantic/site-acceptance RU normalization depth. Worse on all three dimensions: more upfront work, contradicts the plan's own non-goals, and locks in undocumented legacy behavior with no current consumer.

## Pareto Front Trade-Offs

- Semantic/site-acceptance RU normalization depth — Trades away byte-level legacy comparability for slice minimality and plan alignment; accepted risk is possible future rework if S08's diffing proves whitespace-sensitive.

**Selection policy:** Prefer the variant that satisfies this slice's acceptance without importing undecided legacy compatibility scope; operator confirmed via AskUserQuestion.

**Recommendation (advisory):** Semantic/site-acceptance RU normalization depth

**Recommendation rationale:** V2 requires committing to legacy quirk-matching before any consumer needs it, directly contradicting the plan's own non-goal list and slice-discipline rules against speculative reuse.


## Comparison Warnings

- ⚠ standard mode comparison proceeds without a parity_plan — declare baseline_set, window, budget, and missing_data_policy
- ⚠ dimension 'slice-minimality' has no polarity; excluding it from Pareto dominance computation
- ⚠ dimension 'plan-alignment' has no polarity; excluding it from Pareto dominance computation
- ⚠ Pareto computation produced no dominance signal because dimensions [future-rework-risk] could not be ordered for any variant pair (scores not in the canonical ordinal vocabulary or persistently missing under the missing_data_policy). Honoring caller-supplied non_dominated_set as authority for explanation coverage; computed Pareto front kept conservative as the full compare set.

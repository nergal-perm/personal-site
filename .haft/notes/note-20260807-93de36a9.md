---
id: note-20260807-93de36a9
kind: Note
version: 1
status: active
title: ProcessTranslationWorkerTest's descendant-stdout timing test is flaky (~2/3 failure rate)
mode: note
valid_until: 2026-11-05T11:31:30Z
created_at: 2026-08-07T11:31:30Z
updated_at: 2026-08-07T11:31:30Z
---

# ProcessTranslationWorkerTest's descendant-stdout timing test is flaky (~2/3 failure rate)

## Observations

- ProcessTranslationWorkerTest.descendantHoldingStdoutCannotKeepTranslationHungAfterParentExit fails intermittently — observed 2 failures out of 3 consecutive `mvn test` runs on 2026-08-07, unrelated to any S09 change.
- This test was added in S08's final-review fix wave (commit e64ac91, 'fix(translation): bound output-drain wait so a stdout-holding descendant cannot hang prepare') and has not been touched since.
- The test spawns a real short-lived process with a stdout-holding descendant and asserts a bounded timeout behavior — likely sensitive to actual OS process scheduling/timing on the test machine, not a logic bug in the production fix itself (the underlying bounded-drain behavior it tests was independently verified correct during S08's review).

## Rationale

Discovered while running S09's final full-suite confirmation before archiving — S09 never touched this file, so this is pre-existing flakiness surfaced incidentally, not a defect this slice introduced or is responsible for fixing.

## Affected Files

- `publication-exporter/src/test/java/dev/eugene/publicationexporter/translation/ProcessTranslationWorkerTest.java`

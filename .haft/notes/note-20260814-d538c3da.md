---
id: note-20260814-d538c3da
kind: Note
version: 1
status: active
title: S18 — Direct-target source-ID admission: resolution (problem close pending operator authorization)
context: Governed by openspec/implementation-plan.md's greenfield exporter slice plan, sub-problem of prob-20260803-fe9b3011. Written because haft_problem(action="close") for prob-20260814-9d502f85 returned operator_confirmation_required — the MCP server is in cli-only binding mode and closing a problem is a binding governance act it will not perform on model-supplied arguments, and no non-interactive CLI close path exists (haft board's close action lives in its full-screen interactive TUI). This note preserves the resolution; the operator still needs to close prob-20260814-9d502f85 themselves.
mode: note
valid_until: 2026-11-12T07:24:39Z
created_at: 2026-08-14T07:24:39Z
updated_at: 2026-08-14T07:24:39Z
---

# S18 — Direct-target source-ID admission: resolution (problem close pending operator authorization)

## Observations

- Addressed via openspec/changes/archive/2026-08-14-s18-direct-target-source-id-admission/.
- Implemented via subagent-driven-development: Codex Companion Tasks (gpt-5.3-codex-spark for the well-specified initial implementation and both mechanical fix rounds), per-round 4-way parallel review (spec compliance, code quality, /applying-sbpp, /oo-design-heuristics -- gpt-5.6-luna), plus a final whole-branch review (Codex, gpt-5.6-sol, xhigh effort).
- Real findings caught and fixed: (1) LinkResolver collected the raw wikilink target instead of its filename stem into privateTargetStems, silently skipping identity-checking for any path-qualified private link target; (2) PrepareHandler.prepareAfterIdentityCheck had no UncheckedIOException/NoSuchElementException handling around the new PrivateNoteIdentityIndex.from(vaultReader) call; (3) from the final whole-branch review -- the one finding no narrower per-commit review could structurally have caught -- the private-identity check ran unconditionally on every prepare() call regardless of whether the note had any links at all, so an unrelated unreadable/corrupted private note anywhere in the vault could block an otherwise-valid, unrelated prepare() (a bounded-request/ADM-05 violation); fixed with a guard clause skipping the check entirely when no direct private targets exist.
- One parked false-positive finding: a reviewer flagged VaultReader's default listAllNotePaths() method as under-implementing SEM-01; this is design.md's explicit, documented tradeoff (avoids forcing 14 unrelated anonymous test VaultReader implementations to change) and the two real adapters both override it correctly.
- Also fixed in this session as a prerequisite baseline repair, not part of the slice itself: a dash-vs-bash cross-platform portability bug in an unrelated translation-worker timeout test (ProcessTranslationWorkerTest), discovered while establishing the pre-slice green baseline on this machine (migrated from macOS to Linux).
- Full suite green throughout: 802 baseline -> 815 after initial implementation -> 817 after fix round 1 -> 820/820 at completion (including one confirmed pre-existing, unrelated flaky test in FilesystemManagedSiteInstallerTest, re-verified clean).
- 7 commits on master: docs (proposal/spec/design/tasks), 3 implementation commits, 2 review-driven fix rounds, spec sync, archive.
- SDD ledger at .superpowers/sdd/tasks/progress.md has the full round-by-round record and all rulings made during the session; safe to delete once this note and the problem close are both settled.


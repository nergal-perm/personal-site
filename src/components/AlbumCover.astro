---
// Абстрактная обложка Spaces из прототипа (renderMusicIndex/renderAlbum).
interface Props {
  size?: 'default' | 'large' | 'xl';
  alt: string;
}

const { size = 'default', alt } = Astro.props;
const sizeClass = size === 'large' ? 'album-cover--large' : size === 'xl' ? 'album-cover--xl' : '';
---

<div class:list={['album-cover', 'album-cover--spaces', sizeClass]} role="img" aria-label={alt}>
  <span>SPACES</span><i></i>
</div>
